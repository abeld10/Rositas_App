<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">

    <!-- Logo or Image at the top -->
    <ImageView
        android:id="@+id/orderReadyImageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:src="@drawable/logo"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="50dp"
    android:contentDescription="Order Ready Image" />

    <!-- Main Text: "Your Order is Ready!" -->
    <TextView
        android:id="@+id/orderReadyTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Your Order is Ready!"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#424242"
        android:layout_below="@id/orderReadyImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp" />

    <!-- Subtext: "Thank you for dining with us." -->
    <TextView
        android:id="@+id/thankYouTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Thank you for dining with us."
        android:textSize="18sp"
        android:textColor="#757575"
        android:layout_below="@id/orderReadyTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp" />

    <!-- Optional Button: "Continue" or "Back to Menu" -->
    <Button
        android:id="@+id/continueButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back to Menu"
        android:backgroundTint="#FF9800"
    android:textColor="#FFFFFF"
    android:layout_below="@id/thankYouTextView"
    android:layout_centerHorizontal="true"
    android:layout_marginTop="32dp" />

</RelativeLayout>
